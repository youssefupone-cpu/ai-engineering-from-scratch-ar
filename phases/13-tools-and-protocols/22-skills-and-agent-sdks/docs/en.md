# المهارات وحزم SDK للوكلاء - المهارات الإنسانية، AGENTS.md، OpenAI التطبيقات SDK
> MCP يقول "ما هي الأدوات الموجودة؟" المهارات تقول "كيفية القيام بالمهمة". طبقات المكدس 2026 كلاهما. يتم شحن مهارات وكيل Anthropic (المعيار المفتوح، ديسمبر 2025) باسم SKILL.md مع الكشف التدريجي. تطبيقات OpenAI SDK هي MCP بالإضافة إلى البيانات التعريفية لعنصر واجهة المستخدم. AGENTS.md (الآن في أكثر من 60.000+ إعادة شراء) موجود في جذر الريبو كسياق وكيل على مستوى المشروع. يسمي هذا الدرس ما يغطيه كل منها وينشئ الحد الأدنى من حزمة SKILL.md + AGENTS.md التي تنتقل عبر الوكلاء.
**النوع:** تعلم
** اللغات: ** بايثون (stdlib، SKILL.md محلل ومحمل)
**المتطلبات:** المرحلة 13 · 07 (MCP خادم)
**الوقت:** ~45 دقيقة
## أهداف التعلم
- التمييز بين الطبقات الثلاث: AGENTS.md (سياق المشروع)، SKILL.md (المعرفة القابلة لإعادة الاستخدام)، MCP (الأدوات).
- اكتب SKILL.md مع YAML المادة الأمامية والإفصاح التدريجي.
- تحميل نمط نظام ملفات المهارات في وقت تشغيل الوكيل.
- قم بتكوين مهارة باستخدام خادم MCP وAGENTS.md بحيث تعمل حزمة واحدة في Claude Code وCursor وCodex.
## المشكلة
يقوم أحد المهندسين بتقطير سير عمل كتابة ملاحظات الإصدار في موجه متعدد الخطوات: "اقرأ أحدث العلاقات العامة المدمجة. مجموعة حسب المنطقة. قم بتلخيص كل منها. اكتب إدخال سجل التغيير وفقًا لأسلوب الفريق. انشر على مسودة Slack." لقد وضعوها في مستند Notion لفريقهم.
يريدون الآن استخدام سير العمل هذا من Claude Code وCursor وCodex CLI. كل وكيل لديه طريقة مختلفة لتحميل التعليمات: أوامر الشرطة المائلة لـ Claude Code، قواعد المؤشر، Codex `.codex.md`. يقوم المهندس بنسخ سير العمل ثلاث مرات ويحتفظ بثلاث نسخ.
AGENTS.md و SKILL.md معًا يصلحان هذا:
- **AGENTS.md** موجود في جذر الريبو. يقرأها كل وكيل متوافق عند بدء الجلسة. "كيف يعمل هذا المشروع؟ ما هي الاتفاقيات؟ ما هي الأوامر التي تجري الاختبارات؟"
- **SKILL.md** عبارة عن حزمة محمولة: YAML المادة الأمامية (الاسم والوصف) + نص تخفيض السعر + الموارد الاختيارية. يقوم الوكلاء الذين يدعمون المهارات بتحميلها بالاسم عند الطلب.
- **MCP** (المرحلة 13 · 06-14) يتعامل مع الأدوات التي تحتاج المهارة إلى استدعائها.
ثلاث طبقات، قطعة أثرية واحدة محمولة.
##المفهوم
### AGENTS.md (agents.md)
تم إطلاقه في أواخر عام 2025، وتبعه أكثر من 60,000 إعادة شراء بحلول أبريل 2026. ملف واحد في جذر الريبو. شكل:
```markdown
# Project: my-service

## Conventions
- TypeScript with strict mode.
- Use Pydantic for models on the Python side.
- Tests run with `pnpm test`.

## Build and run
- `pnpm dev` for local dev server.
- `pnpm build` for production bundle.
```

يقرأ الوكلاء هذا عند بدء الجلسة ويستخدمونه لمعايرة سلوكهم لهذا المشروع. يدعم كل وكيل ترميز في عام 2026 AGENTS.md: Claude Code، Cursor، Codex، Copilot Workspace، opencode، Windsurf، Zed.
### تنسيق SKILL.md
مهارات الوكيل الأنثروبي (تم إصدارها كمعيار مفتوح في ديسمبر 2025):
```markdown
---
name: release-notes-writer
description: Write a changelog entry for the latest merged PRs following this project's style.
---

# Release notes writer

When invoked, run these steps:

1. List PRs merged since the last tag. Use `gh pr list --base main --state merged`.
2. Group by label: feature, fix, chore, docs.
3. For each PR in each group, write one line: `- <title> (#<num>)`.
4. Draft the release notes and stage them in CHANGELOG.md.

If the user says "ship", run `git tag vX.Y.Z` and `gh release create`.

## Notes

- Never include commits without a PR.
- Skip "chore" entries from the public changelog.
```

تعلن Frontmatter عن هوية المهارة. الجسم هو الموجه الذي يظهر للنموذج عند تحميل المهارة.
### الكشف التدريجي
يمكن أن تشير المهارات إلى الموارد الفرعية التي يجلبها الوكيل فقط عند الحاجة. مثال:
```
skills/
  release-notes-writer/
    SKILL.md
    style-guide.md
    template.md
    scripts/
      generate.sh
```

يقول SKILL.md "راجع style-guide.md للتعرف على قواعد النمط." يقوم الوكيل بسحب style-guide.md فقط عندما تكون المهارة قيد التشغيل بشكل نشط. يؤدي هذا إلى تجنب تضخيم الموجه بالتفاصيل التي قد لا يحتاجها النموذج.
### اكتشاف نظام الملفات
تقوم أوقات تشغيل الوكيل بفحص الدلائل المعروفة لملفات SKILL.md:
- __الكود_0__
- مشروع `./skills/*/SKILL.md`
- __الكود_2__
يتم التحميل حسب اسم المجلد والموضوع الأمامي `name`. يتبع كل من Claude Code وAnthropic Claude Agent SDK وSkillKit (وكيل مشترك) هذا النمط.
### وكيل كلود الأنثروبي SDK
يقوم `@anthropic-ai/claude-agent-sdk` (TypeScript) و`claude-agent-sdk` (Python) بتحميل المهارات عند بداية الجلسة، ويعرضها كـ "وكلاء" قابلين للاستدعاء داخل وقت التشغيل. ترسل حلقة الوكيل إلى المهارة عندما يستدعيها المستخدم.
### OpenAI التطبيقات SDK
تم إطلاقه في أكتوبر 2025؛ مبني مباشرة على MCP. توحيد موصلات OpenAI السابقة وإجراءات GPT المخصصة ضمن سطح مطور واحد. تطبيق Apps SDK هو:
- خادم MCP (الأدوات والموارد والمطالبات).
- بالإضافة إلى البيانات الوصفية لعنصر واجهة المستخدم لـ ChatGPT's UI.
- بالإضافة إلى مورد MCP Apps `ui://` الاختياري للأسطح التفاعلية.
نفس البروتوكول، أكثر ثراءً UX.
### إمكانية النقل بين الوكلاء عبر SkillKit
تعمل أدوات مثل SkillKit وطبقات التوزيع المشابهة عبر الوكلاء على ترجمة SKILL.md واحد إلى التنسيق الأصلي لكل من أكثر من 32 وكيل AI (Claude Code، وCursor، وCodex، وGemini CLI، وOpenCode، وما إلى ذلك). مصدر واحد للحقيقة؛ العديد من المستهلكين.
### المكدس ثلاثي الطبقات
| طبقة | ملف | تم التحميل عندما | الغرض |
|-------|------|------------|---------|
| AGENTS.md | جذر الريبو | بداية الجلسة | اتفاقيات على مستوى المشروع |
| SKILL.md | دليل المهارات | تم استدعاء المهارة | سير العمل القابل لإعادة الاستخدام |
| MCP الخادم | عملية خارجية | الأدوات اللازمة | إجراءات قابلة للاستدعاء |
يؤلف الثلاثة جميعًا: يقرأ الوكيل AGENTS.md عند بداية الجلسة، ويستدعي المستخدم مهارة، وتتضمن تعليمات المهارة استدعاءات أداة MCP، ويرسل الوكيل عبر عميل MCP.
## استخدمه
`code/main.py` يشحن محلل ومحمل stdlib SKILL.md. يكتشف المهارات تحت `./skills/`، ويوزع YAML المادة الأمامية بالإضافة إلى نص تخفيض السعر، وينتج إملاءًا مرتبطًا باسم المهارة. ثم يقوم بمحاكاة حلقة الوكيل التي تستدعي `release-notes-writer` بالاسم.
ما الذي يجب النظر إليه:
- تم تحليل المادة الأمامية YAML باستخدام الحد الأدنى من المحلل اللغوي stdlib (بدون تبعية `pyyaml`).
- هيئة المهارة المخزنة حرفيا. يقوم الوكيل بإضافته مسبقًا إلى موجه النظام عند الاستدعاء.
- تم عرض الكشف التدريجي عبر وظيفة `read_subresource` التي تسحب الملفات المرجعية عند الطلب.
## اشحنها
ينتج هذا الدرس `outputs/skill-agent-bundle.md`. بالنظر إلى سير العمل، تنتج المهارة حزمة مخطط الخادم المدمجة SKILL.md + AGENTS.md + MCP، القابلة للنقل عبر الوكلاء.
## تمارين
1. قم بتشغيل `code/main.py`. أضف مهارة ثانية ضمن `skills/` وتأكد من أن المُحمل يلتقطها.
2. اكتب AGENTS.md لهذا المقرر الدراسي. قم بتضمين أوامر الاختبار واصطلاحات الأسلوب والنموذج العقلي للمرحلة 13.
3. قم بنقل سير عمل متعدد الخطوات من المستندات الداخلية لفريقك إلى SKILL.md. تحقق من أنه يتم تحميله في Claude Code.
4. قم بترجمة المهارة إلى تنسيقات القواعد الأصلية الخاصة بـ Cursor و Codex يدويًا. قم بإحصاء الاختلافات بين التنسيقات - هذا هو سطح الترجمة الذي تقوم SkillKit بأتمتةه.
5. اقرأ منشور مدونة مهارات الوكيل الأنثروبي. حدد ميزة واحدة في Claude Agent SDK لا يغطيها محمل هذا الدرس. (تلميح: استدعاء الوكيل الفرعي.)
## المصطلحات الرئيسية
| مصطلح | ماذا يقول الناس | ماذا يعني في الواقع |
|------|----------------|-----------------------|
| SKILL.md | "ملف المهارة" | YAML المادة الأمامية بالإضافة إلى نص تخفيض السعر، المحمل بواسطة وقت تشغيل الوكيل |
| AGENTS.md | "سياق وكيل الريبو الجذر" | قراءة ملف الاتفاقيات على مستوى المشروع عند بدء الجلسة |
| الكشف التدريجي | "الموارد الفرعية ذات التحميل البطيء" | يتم سحب الملفات المرجعية لجسم المهارة فقط عند الحاجة إليها |
| فرونت ماتر | "YAML كتلة في الأعلى" | البيانات الوصفية (الاسم والوصف) في محددات `---` |
| وكيل كلود SDK | "وقت تشغيل المهارة الإنسانية" | `@anthropic-ai/claude-agent-sdk`، يقوم بتحميل المهارات والطرق |
| OpenAI التطبيقات SDK | "MCP + تعريف القطعة" | سطح تطوير OpenAI مبني على خطافات MCP بالإضافة إلى ChatGPT UI |
| اكتشاف المهارات | "فحص نظام الملفات" | السير على الإرشادات المعروفة لـ SKILL.md، المفتاح بالاسم |
| قابلية النقل عبر الوكيل | "مهارة واحدة العديد من العملاء" | ترجمة SKILL.md واحد إلى أكثر من 32 وكيلًا عبر أدوات بأسلوب SkillKit |
| مهارة الوكيل | "الدراية المحمولة" | قالب مهمة قابل لإعادة الاستخدام خارج مفهوم أداة MCP |
| التطبيقات SDK | "MCP بالإضافة إلى ChatGPT UI" | تم توحيد الموصلات ونقاط GPT المخصصة في MCP |
## مزيد من القراءة
- [Anthropic — Agent Skills announcement](https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills) — الإطلاق في ديسمبر 2025
- [Anthropic — Agent Skills docs](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/overview) — مرجع تنسيق SKILL.md
- [OpenAI — Apps SDK](https://developers.openai.com/apps-sdk) — منصة المطورين المستندة إلى MCP لـ ChatGPT
- [agents.md](https://agents.md/) — تنسيق AGENTS.md وقائمة الاعتماد
- [Anthropic — anthropics/skills GitHub](https://github.com/anthropics/skills) — أمثلة على المهارات الرسمية
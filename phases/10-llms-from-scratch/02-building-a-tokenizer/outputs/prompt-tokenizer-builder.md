---
name: prompt-tokenizer-builder
description: Build and debug production-quality tokenizers for LLM projects
version: 1.0.0
phase: 10
lesson: 2
tags: [tokenizer, bpe, byte-level, special-tokens, chat-template, multilingual]
---

# Production Tokenizer Builder

عند إنشاء أو تصحيح أخطاء رمز مميز لمشروع LLM، اتبع إطار العمل هذا.

## Pipeline Checklist

يحتاج كل رمز مميز للإنتاج إلى هذه المراحل الخمس. إذا كان أحد المفقودين، سوف تصل إلى الحالات المتطورة في الإنتاج.

1. **تطبيع** - تطبيق NFKC تطبيع Unicode. يؤدي هذا إلى طي الحروف المركبة ("fi" -> "fi")، وتطبيع الأحرف ذات العرض الكامل، وتوحيد المسافات البيضاء. تخطي هذا وستحصل الكلمة نفسها على معرفات رمزية مختلفة اعتمادًا على كيفية كتابتها.

2. **الترميز المسبق** - تقسيم النص إلى أجزاء قبل BPE. استخدم النمط العادي GPT-2 للنماذج التي تركز على اللغة الإنجليزية. استخدم أسلوب البايت الخام الخاص بـ SentencePiece للنماذج متعددة اللغات. يحدد الاختيار ما إذا كان BPE يمكن دمجه عبر حدود الكلمات.

3. **BPE دمج** - قم بتطبيق جدول الدمج الذي تم تعلمه على تسلسلات البايت داخل كل قطعة. جدول الدمج IS المعرفة المكتسبة للرمز المميز. كل شيء آخر هو السباكة.

4. **حقن الرموز المميزة** - قم بمطابقة الرموز المميزة تمامًا قبل تشغيل BPE. [BOS]، [EOS]، [PAD]، تحصل علامات قالب الدردشة على معرفات ثابتة. لا يشاركون أبدًا في عمليات الدمج.

5. **ID رسم الخرائط** - تحويل سلاسل الرمز إلى أعداد صحيحة. يرى النموذج الأعداد الصحيحة فقط.

## Debugging Tokenizer Issues

**العَرَض: يُنتج النموذج بيانات غير مرغوب فيها عند إدخال الدردشة**
- التحقق من قالب الدردشة. كل نموذج له شكل مختلف. يستخدم Llama 3 علامات `<|start_header_id|>`. يستخدم ChatGPT علامات `<|im_start|>`. القالب الخاطئ يضع المدخلات خارج توزيع التدريب.

**العَرَض: يستخدم النص غير الإنجليزي عددًا كبيرًا جدًا من الرموز المميزة**
- التحقق من الخصوبة (الرموز لكل كلمة). أعلى من 2.0 يعني أن أداة الرمز المميز تهدر نافذة سياق تلك اللغة. الحلول: أعد التدريب باستخدام المزيد من البيانات متعددة اللغات، أو قم بزيادة حجم المفردات، أو استخدم SentencePiece مع Unigram.

**العَرَض: فشل الأرقام والحسابات**
- تحقق من كيفية ترميز digits. "1234" كرمز واحد يعني أن النموذج لا يمكنه القيام بعمليات على مستوى digit. قم بتقسيم digits بشكل فردي أثناء عملية الترميز المسبق.

**العَرَض: الرموز المميزة للكود غير فعالة**
- التحقق من كيفية التعامل مع المسافة البادئة. GPT-2 يهدر الرمز المميز الرموز المميزة في المساحات. يستخدم Codex وStarCoder رموز مسافة بادئة خاصة (4 مسافات = رمز مميز واحد).

## Vocabulary Size Decision

- 32 ألف رمز: لغة واحدة، نموذج صغير، حوسبة محدودة. طبقة التضمين هي 32 كيلو بايت * معلمات d_model.
- 50K-64K: متعدد اللغات أو كثيف التعليمات البرمجية. توازن جيد لمعظم المشاريع.
- 100 ألف+ (GPT-4، اللاما 3): فقط مع بيانات تدريب ضخمة. تسلسلات أقصر لكن معلمات التضمين 100K * d_model.

بالنسبة لنموذج ذو 4096 بُعدًا: 32 ألف كلمة = 131 مليون معلمة تضمين. 128 ألف كلمة = 524 مليون معلمة للتضمين. هذا هو 400 مليون معلمة فقط في طبقة التضمين.

## Speed Requirements

- ترميز بيانات التدريب: استخدم المكتبات المدعومة Rust (tiktoken، وHuggingFace tokenizers). Pure Python أبطأ بمقدار 10-100 مرة.
- ترميز الاستدلال: زمن الاستجابة أقل أهمية (تسلسل واحد)، ولكن لا يزال يستخدم التطبيقات المجمعة.
- المعيار: ترميز 1 غيغابايت من النص وقياس وقت ساعة الحائط. إذا استغرق الأمر أكثر من 60 ثانية، قم بالتبديل إلى الواجهة الخلفية Rust.

## Chat Template Validation

قبل نشر أي نموذج دردشة، تحقق من القالب:

1. قم بتشفير محادثة معروفة باستخدام الرمز المميز
2. فك تشفيرها وإعادتها إلى النص
3. قارن حرفًا بحرف بالتنسيق المتوقع من وثائق النموذج
4. انتبه إلى: الأسطر الجديدة بعد الرموز المميزة للرأس، والمسافات قبل المحتوى، وعلامات نهاية المنعطف
5. حالات اختبار الحافة: رسالة نظام فارغة، ورسالة مستخدم طويلة جدًا، ودورات متعددة للمساعد

يعد الخطأ في قالب الدردشة هو المصدر الأكثر شيوعًا لأداء نموذج الدردشة المتدني.

# CrewAI: الأطقم والتدفقات القائمة على الأدوار
> CrewAI هو إطار عمل متعدد الوكلاء قائم على الأدوار لعام 2026. أربعة أساسيات: الوكيل، المهمة، الطاقم، العملية. شكلان من المستوى الأعلى: الأطقم (التعاون المستقل والمبني على الأدوار) والتدفقات (المعتمدة على الأحداث والحتمية). المستندات صريحة: "بالنسبة لأي تطبيق جاهز للإنتاج، ابدأ بالتدفق."
** النوع: ** تعلم + بناء
** اللغات: ** بايثون (stdlib)
**المتطلبات الأساسية:** المرحلة 14 · 12 (أنماط سير العمل)، المرحلة 14 · 14 (نموذج الممثل)
**الوقت:** ~75 دقيقة
## أهداف التعلم
- قم بتسمية العناصر الأربعة الأساسية لـ CrewAI (الوكيل، المهمة، الطاقم، العملية) وما يملكه كل منهم.
- التمييز بين عملية الإجماع التسلسلية والهرمية والمخطط لها؛ اختر واحدًا لكل عبء عمل.
- تمييز الأطقم (المعتمدة على الأدوار المستقلة) عن التدفقات (الحتمية المستندة إلى الحدث)، وشرح توصيات إنتاج المستندات.
- الأدوات السلكية مع مصمم الديكور `@tool` والفئة الفرعية `BaseTool`؛ السبب حول المخرجات المنظمة مقابل النص الحر.
- قم بتسمية أنواع ذاكرة CrewAI الأربعة ومتى يؤتي كل منها ثماره.
- تنفيذ طاقم مكون من ثلاثة وكلاء من stdlib (الباحث والكاتب والمحرر) الذي ينتج ملخصًا.
- حدد أوضاع فشل CrewAI الثلاثة: المطالبة بالانتفاخ، والمدير-LLM الضريبة، وعمليات التسليم الهشة.
## المشكلة
الفرق التي تتبنى أطر عمل متعددة الوكلاء تصطدم بنفس الجدار. يبدو "التعاون المستقل" رائعًا في العرض التوضيحي. ثم يقوم العميل بتسجيل خطأ وتحتاج إلى إعادة تشغيل حتمية. أو يسأل قسم الشؤون المالية عن تكلفة الطاقم الذي يتم توجيهه بواسطة LLM لكل عملية تشغيل. أو يحتاج عند الطلب إلى معرفة الوكيل الذي توقف عند 3 AM.
النموذج الحر LLM- لا تجيب الطواقم الموجهة على أي من هذه الأسئلة بشكل نظيف. تجيب DAGs النقية على كل هذه الأسئلة ولكنها تفقد الشكل الاستكشافي الذي يحتاجه وكيل العصف الذهني.
إن تقسيم CrewAI صادق فيما يتعلق بالتجارة. أطقم للعمل التعاوني القائم على الأدوار والاستكشاف. التدفقات للإنتاج القائم على الأحداث والمملوك للكود والقابل للتدقيق. نفس الإطار، شكلان، اختر كل سطح.
##المفهوم
### أربعة البدائيات
سطح CrewAI صغير. احفظ هذا والباقي هو التكوين.
- **الوكيل.** `role + goal + backstory + tools + (optional) llm`. الخلفية الدرامية حاملة. إنه يشكل النغمة والحكم عندما يتوقف الوكيل. الأدوات هي وظائف يمكن للوكيل الاتصال بها (المزيد أدناه).
- **المهمة.** `description + expected_output + agent + (optional) context + (optional) output_pydantic`. وحدة عمل قابلة لإعادة الاستخدام. `expected_output` هو العقد. يسرد `context` المهام الأولية التي يتم تمرير مخرجاتها. `output_pydantic` يفرض شكلاً منظمًا.
- **الطاقم.** الحاوية. يمتلك قائمة `agents`، وقائمة `tasks`، و`process`، ​​والإعدادات الاختيارية `memory` + `verbose` + `manager_llm`.
- **العملية.** استراتيجية التنفيذ. متسلسل، هرمي، إجماع (مخطط له). يختار شكل المدى.
الوكلاء لا يرون بعضهم البعض بشكل مباشر. مهام الوكلاء المرجعيين. يقوم الطاقم بتسلسل المهام. تقرر العملية من يختار المهمة التالية. هذا هو النموذج العقلي بأكمله.
> **تم التحقق من صحته مقابل** CrewAI 0.86 (2026-05). قد تقوم الإصدارات الأحدث بإعادة تسمية أنواع العمليات أو دمجها؛ تحقق من [CrewAI Processes docs](https://docs.crewai.com/concepts/processes) قبل الاعتماد على شكل معين.
### التسلسل الهرمي مقابل الإجماع
- **متسلسلة.** يتم تشغيل المهام بترتيب الإعلان. مخرجات المهمة N متاحة كـ `context` للمهمة N+1. بأقل تكلفة. الأكثر توقعا. استخدم عندما يتم إصلاح الطلب.
- **تسلسل هرمي.** يقوم وكيل المدير (استدعاء LLM منفصل) بتوجيه المسارات بين المتخصصين. يقوم CrewAI بإنشاء المدير إما من تكوين `manager_llm` الخاص بك أو من الإعداد الافتراضي. يختار المدير المهمة التالية في كل جولة ويمكنه رفضها أو إعادة توجيهها. استخدمه عندما يكون لديك أربعة متخصصين أو أكثر ويعتمد الطلب حقًا على المخرجات السابقة.
- **الإجماع.** تم التخطيط له، ولم يتم تنفيذه حاليًا بشكل عام API. تحتفظ المستندات بالاسم للعملية المستقبلية القائمة على التصويت. لا تعتمد عليه اليوم.
يضيف التسلسل الهرمي مكالمة LLM لكل جولة (المدير) أعلى كل مكالمة متخصصة. يمكن أن تتضاعف تكلفة الرمز المميز ثلاث مرات في خمس خطوات. ادفع ثمنها فقط عندما تحتاج إلى التوجيه.
### أطقم العمل مقابل التدفقات
هذا هو الإطار الذي ستؤدي إليه المستندات في عام 2026.
- **الطاقم.** LLM استقلالية مدفوعة. يختار الإطار الشكل في وقت التشغيل. مناسب لـ: البحث، والعصف الذهني، والمسودات الأولى، وفي أي مكان يكون المسار جزءًا من الإجابة. من الصعب اعادتها. من الصعب اختبارها. رخيصة للنموذج الأولي.
- **التدفق.** رسم بياني يحركه الحدث تملكه. `@start` يمثل الإدخال. `@listen(topic)` يمثل خطوة يتم تشغيلها عندما تبث خطوة أخرى هذا الموضوع. كل خطوة هي لغة بايثون بسيطة (يمكن استدعاء الطاقم داخليًا). جيد ل: الإنتاج. يمكن ملاحظتها. قابل للاختبار. حتمية.
توصية إنتاج المستندات لعام 2026: ابدأ بالتدفق. قم بطي الطاقم كـ `Crew.kickoff()` يستدعي من داخل خطوات التدفق عندما تستحق الاستقلالية تكلفتها. يمنحك التدفق مسار التدقيق، بينما يمنحك الطاقم الاستكشاف. يؤلف، لا تختار.
### تكامل الأداة
ثلاث طرق لمنح الوكيل أداة. اختر أبسط ما يناسبك.
1. **`@tool` الديكور.** الوظائف البحتة تصبح أدوات. التوقيع هو المخطط. docstring هو الوصف الذي يراه LLM. الأفضل للمساعدين لمرة واحدة.
   ```python
   from crewai.tools import tool

   @tool("Search the web")
   def search(query: str) -> str:
       """Return top results for the query."""
       return run_search(query)
   ```

2. **`BaseTool` فئة فرعية.** أداة قائمة على الفئة مع مخطط وسيطات صريح، ودعم غير متزامن، وإعادة المحاولة. يُستخدم عندما تكون الأداة في حالة (عميل، ذاكرة تخزين مؤقت) أو تحتاج إلى وسيطات منظمة.
   ```python
   from crewai.tools import BaseTool
   from pydantic import BaseModel

   class SearchArgs(BaseModel):
       query: str
       limit: int = 10

   class SearchTool(BaseTool):
       name = "web_search"
       description = "Search the web and return top results."
       args_schema = SearchArgs

       def _run(self, query: str, limit: int = 10) -> str:
           return self.client.search(query, limit=limit)
   ```

3. **مجموعات الأدوات المدمجة.** يقوم CrewAI بشحن محولات الطرف الأول: `SerperDevTool`، `FileReadTool`، `DirectoryReadTool`، `CodeInterpreterTool`، `RagTool`، `WebsiteSearchTool`. سلكي باستيراد واحد.
تستخدم المخرجات المنظمة Pydantic. قم بتمرير `output_pydantic=MyModel` على المهمة. يتحقق CrewAI من صحة استجابة LLM مقابل النموذج ويقوم إما بالإكراه أو إعادة المحاولة. قم بإقران هذا بسلسلة `expected_output` ضيقة. تعد مخرجات النص الحر مناسبة للمسودات؛ المخرجات المنظمة هي ما يمكن أن تستهلكه التدفقات النهائية.
### خطافات الذاكرة
يقوم CrewAI بشحن أربعة أنواع من الذاكرة خارج الصندوق. إنهم يؤلفون: يمكن للطاقم تمكين الأربعة جميعًا في وقت واحد.
> **تم التحقق من صحته مقابل** CrewAI 0.86 (2026-05). تقوم الإصدارات الأخيرة بتوجيه كل شيء من خلال نظام `Memory` الموحد الذي يغطي هذه المتاجر الأربعة. لا يزال النموذج المفاهيمي أدناه قائمًا، ولكن قد ينهار سطح الطبقة العامة إلى نقطة دخول واحدة `Memory` في الإصدارات الأحدث؛ حدد [CrewAI memory docs](https://docs.crewai.com/concepts/memory) لمعرفة API الحالي.
- **على المدى القصير.** المخزن المؤقت للمحادثة خلال فترة تشغيل واحدة. تمحى في النهاية.
- **على المدى الطويل.** استمر عبر فترات التشغيل. مخزنة في ناقل DB (Chroma بشكل افتراضي، قابل للتبديل). تم الاسترجاع عن طريق التشابه مع المهمة الحالية.
- **الكيان.** حقائق لكل كيان. "العميل X موجود في خطة المؤسسة." مرتبط بالكيان وليس بالتشابه. البقاء على قيد الحياة عبر الجري.
- **سياقي.** استرجاع وقت التجميع. تسحب الذاكرة ذات الصلة في اللحظة التي يحتاج فيها الوكيل إليها، وليست محملة مسبقًا.
قم بالتمكين على الطاقم باستخدام `memory=True` أو التكوين لكل نوع. مدعومة بموفر التضمين الذي قمت بتكوينه (الإعداد الافتراضي هو OpenAI، قابل للتبديل إلى محلي). تعد الذاكرة واحدة من الأماكن التي تكتسب فيها CrewAI مكانتها في مواجهة الأطر الرقيقة؛ يتطلب منك LangGraph النقي توصيل كل منها بنفسك.
### عندما يناسب CrewAI
- ثلاثة إلى ستة وكلاء بأدوار محددة وسير عمل تعاوني. الصياغة، المراجعة، التخطيط، العصف الذهني.
- التوجيه حيث يكون حكم LLM بشأن الخطوة التالية جزءًا من القيمة (الهرمية).
- في أي مكان يكون الفريق أكثر سعادة عند قراءة `role + goal + backstory` بدلاً من قراءة تعريف الرسم البياني.
### عندما لا يكون CrewAI مناسبًا
- DAGs حتمية مع ترتيب صارم. استخدم LangGraph (الدرس 13). شكل الرسم البياني هو التجريد الصحيح؛ إن إطار دور CrewAI هو الاحتكاك.
- ميزانيات الكمون الفرعية الثانية. يضيف التسلسل الهرمي رحلات ذهابًا وإيابًا. حتى أن Sequential يقوم بتسلسل المطالبات التي تتضمن قصصًا درامية ومخرجات سابقة.
- حلقات وكيل واحد. تخطي الإطار؛ حلقة الوكيل (الدرس 1) بالإضافة إلى أن تسجيل الأداة أقصر.
يوضح الدرس 17 (مقايضات إطار عمل الوكيل) ذلك في مصفوفة. النسخة القصيرة: CrewAI تقع في الزاوية "المعتمدة على الأدوار التعاونية".
### شكل التبعية
مستقلة عن لانجشين. بايثون 3.10 إلى 3.13. يستخدم `uv`. عدد النجوم: راجع [crewAIInc/crewAI](https://github.com/crewAIInc/crewAI) (لقطة اعتبارًا من 2026-05). AWS تم توثيق التكامل الأساسي؛ تشير معايير البائعين إلى تسريع كبير مقارنة بـ LangGraph في أعباء عمل QA، ولكن لم يتم نشر المنهجية (مجموعة البيانات، الأجهزة، مقياس التقييم)، لذا تعامل مع أرقام بائعي إطار العمل باعتبارها توجيهية فقط.
### أين يسير هذا النمط بشكل خاطئ
- **التضخم الفوري من القصص الدرامية.** قصة درامية مكونة من 2000 كلمة لكل وكيل وطاقم مكون من خمسة وكلاء يحرق ميزانية السياق قبل استدعاء الأداة الأول. احتفظ بالخلفيات الدرامية أقل من 200 كلمة. إعادة استخدام العبارات عبر الوكلاء؛ لا تكرر أسلوب المنزل خمس مرات.
- **ضريبة الرمز المميز للمدير-LLM.** تضيف العملية الهرمية مكالمة مدير LLM قبل كل مكالمة متخصصة. في طاقم مكون من خمس مهام، يكون هناك ستة مكالمات LLM بدلاً من خمسة، ويحمل استدعاء المدير قائمة المهام الكاملة بالإضافة إلى المخرجات السابقة. قم بالتبديل إلى التسلسلي ما لم يعتمد التوجيه على الإخراج.
- **عمليات تسليم هشة.** المهمة N `expected_output` هي "مخطط تفصيلي". تقرأها المهمة N+1 كـ `context` وتحاول تحليل ثلاثة أقسام. أنتج LLM أربعة. ad-libs وكيل المصب. قم بالإصلاح باستخدام `output_pydantic` في المهمة N حتى تقرأ المهمة N+1 كائنًا مكتوبًا، وليس نصًا حرًا.
- **الطاقم كمنتج.** يتم شحن الطاقم ذو الشكل الحر إلى الإنتاج بدون غلاف التدفق. تقلب الناتج مرتفع. الإعادة مستحيلة. عند الطلب لا يمكن التمييز بين المدى السيئ والجري الجيد. التفاف مع التدفق.
## بنائها
`code/main.py` يطبق إصدارات stdlib من كلا الشكلين بالإضافة إلى طاقم مكون من ثلاثة وكلاء.
شكل:
- `Agent`، `Task` فئات البيانات المطابقة لسطح CrewAI.
- `SequentialCrew.kickoff(inputs)` يقوم بتشغيل المهام بترتيب الإعلان، ويخرج الترابط كـ `context`.
- `HierarchicalCrew.kickoff(topic)` يضيف وكيلًا إداريًا يختار التخصص التالي في كل جولة، ويتوقف عند "تم".
- `Flow` مع `@start` و`@listen(topic)` المزخرفة، وحلقة حدث صغيرة، وتتبع.
- `tool(name)` مصمم الديكور يعكس شكل `@tool` الخاص بـ CrewAI.
- `Memory` مع متاجر `short_term`، `long_term`، `entity`؛ يستخدم التشابه المستهزئ numpy.
- الاستجابات الوهمية LLM عبارة عن سلاسل مشفرة بمفتاح خارج الدور بالإضافة إلى بادئة الإدخال. لا توجد شبكة. حتمية.
عرض توضيحي ملموس: باحث وكاتب وطاقم تحرير ينتجون ملخصًا عن "Agent Engineering 2026". - الباحث يسحب (استهزأ) بالمصادر. مسودات الكاتب. يشدد المحرر. يمر نفس الطاقم عبر التدفق لإظهار الشكل الحتمي.
تشغيله:
```bash
python3 code/main.py
```

يغطي التتبع: مخرجات تسلسلية للطاقم من خلال `context`، طاقم هرمي مع اختيارات المدير (الباحث، الكاتب، المحرر، ثم "تم")، تدفق تشغيل نفس الخطوات الثلاث مع موضوعات صريحة (`researched`، `drafted`، `edited`)، استدعاءات الأداة التي يتم توجيهها من خلال `@tool`، والذاكرة طويلة المدى الباقية عبر ركلتين.
أثر الطاقم سائل. يمكن للمدير من حيث المبدأ إعادة الطلب. تم إصلاح تتبع التدفق. هذا الاختيار هو الدرس.
## استخدمه
- **CrewAI Flow** للإنتاج. حتى عندما يكون التدفق عبارة عن خطوة واحدة تستدعي `Crew.kickoff()`. التدفق يعطي حدود التدقيق.
- **CrewAI Crew (متسلسل)** للعمل التعاوني ذو الترتيب الواضح، وخاصة المسودات الأولى وحلقات المراجعة.
- **CrewAI Crew (هرمي)** عندما يعتمد التوجيه على المخرجات ويكون لديك أربعة متخصصين أو أكثر.
- **LangGraph** (الدرس 13) لآلات الحالة الواضحة، والسيرة الذاتية الدائمة، والترتيب الصارم.
- **AutoGen v0.4** (الدرس 14) لتزامن نموذج الممثل وعزل الأخطاء.
- **OpenAI الوكلاء SDK** (الدرس 16) لـ OpenAI-المنتجات الأولى ذات عمليات التسليم وحواجز الحماية.
- **Claude Agent SDK** (الدرس 17) لمنتجات Claude-first مع الوكلاء الفرعيين ومتجر الجلسة.
## اشحنها
`outputs/skill-crew-or-flow.md` يختار الطاقم مقابل التدفق لمهمة ويدعم الحد الأدنى من التنفيذ. رفض شديد لموضوعات الطاقم بدون خلفية درامية، والتدفق بدون موضوعات صريحة، والتسلسل الهرمي مع أقل من ثلاثة متخصصين.
## مطبات
- **القصة الدرامية كنكهة.** فهي تشكل المخرجات. اختبار ثلاثة متغيرات لكل وكيل؛ التباين حقيقي. اختر واحدًا، وقم بتجميده.
- **تخطي `expected_output`.** بدون عقد لكل مهمة، تلتقط المهام النهائية أي شيء تم إنتاجه بواسطة LLM. يدير الطاقم. فشل التدقيق.
- **الذاكرة تعمل دائمًا.** تكتب على المدى الطويل في كل مرة يتم تشغيلها. المتجه DB ينمو. يصبح الاسترجاع صاخبًا. يكتب النطاق إلى المهام التي تكون فيها الحقيقة ثابتة.
- ** انجراف موجه المدير. ** موجه مدير التسلسل الهرمي ضمني. إذا أصبح التوجيه غريبًا، فقم بتفريغه في الوضع المطول واقرأه.
- **الآثار الجانبية للأداة في الطاقم.** يمكن للطاقم استدعاء الأداة مرات أكثر من المتوقع. POST، DELETE، الدفع يتم في خطوة التدفق، وليس في أداة الطاقم أبدًا.
## تمارين
1. تحويل الطاقم المتسلسل إلى تدفق. قم بإحصاء نقاط الاتصال التي ينخفض ​​فيها التباين. لاحظ أين انخفضت إمكانية القراءة.
2. أضف ذاكرة الكيان إلى الطاقم: تستمر الحقائق المتعلقة بالعميل طوال المباراة. التحقق من الاسترجاع يسحب الكيان الصحيح.
3. قم بتنفيذ عملية هرمية حيث يرفض المدير التوجه إلى المحرر حتى يحتوي مخرج الكاتب على ثلاث فقرات على الأقل. تتبع إعادة المحاولة.
4. قم بتوصيل فئة فرعية `BaseTool` لبحث الويب (السخرية). قارن شكل التتبع بإصدار الديكور `@tool`.
5. أضف `output_pydantic=Brief` إلى مهمة المحرر، حيث يحتوي `Brief` على `title`، `summary`، `sections`. جعل إخراج مهمة الكاتب مشوهًا JSON مرة واحدة؛ تحقق من سلوك إعادة المحاولة الخاص بـ CrewAI في التتبع.
6. اقرأ مقدمة مستندات CrewAI. قم بنقل اللعبة إلى `crewai` API الحقيقي. ما هي الضمانات التي تخطاها إصدار stdlib؟
7. قم بتوصيل AgentOps أو Langfuse (الدرس 24) إلى التشغيل الحقيقي. ما هي الآثار التي فاتتك في إصدار stdlib؟
## المصطلحات الرئيسية
| مصطلح | ماذا يقول الناس | ماذا يعني في الواقع |
|------|----------------|-----------------------|
| الوكيل | "الشخصية" | الدور + الهدف + الخلفية الدرامية + الأدوات |
| مهمة | "وحدة العمل" | الوصف + المخرجات المتوقعة + المكلف + المخرجات المنظمة الاختيارية |
| الطاقم | "فريق الوكيل" | حاوية الوكلاء + المهام + العملية |
| عملية | "استراتيجية التنفيذ" | متسلسل / هرمي / إجماع (مخطط) |
| التدفق | "سير العمل الحتمي" | مبني على الأحداث، ومملوك للكود، وقابل للاختبار |
| الخلفية الدرامية | "موجه الشخصية" | النغمة والحكم المشكل للوكيل |
| `@tool` | "أداة الوظيفة" | الديكور الذي يحول الوظيفة إلى أداة يمكن للوكيل الاتصال بها |
| __الكود_1__ | "أداة الفصل" | أداة قائمة على الفصل مع مخطط args، وإعادة المحاولة، ودعم غير متزامن |
| ذاكرة الكيان | "حقائق لكل كيان" | الذاكرة مخصصة للعميل/الحساب/المشكلة |
| الذاكرة طويلة المدى | "الذاكرة المتقاطعة" | الذاكرة المدعومة بالنواقل التي تبقى بين ركلات البداية |
| الذاكرة السياقية | "الاسترجاع في الوقت المناسب" | يتم سحب الذاكرة في اللحظة التي يحتاج فيها الوكيل إليها |
| مدير LLM | "وكيل جهاز التوجيه" | LLM إضافي في العملية الهرمية التي تختار المهمة التالية |
| __الكود_2__ | "عقد المهمة" | السلسلة التي تخبر الوكيل (والتدقيق) بالشكل الذي سيتم إرجاعه |
## مزيد من القراءة
- [CrewAI docs introduction](https://docs.crewai.com/en/introduction): المفاهيم ومسار الإنتاج الموصى به
- [CrewAI Flows guide](https://docs.crewai.com/en/concepts/flows): شكل يحركه الحدث، `@start`، `@listen`
- [CrewAI tools reference](https://docs.crewai.com/en/concepts/tools): `@tool`، `BaseTool`، مجموعات الأدوات المدمجة
- [CrewAI memory](https://docs.crewai.com/en/concepts/memory): قصير المدى، طويل المدى، كيان، سياقي
- [Anthropic, Building Effective Agents](https://www.anthropic.com/research/building-effective-agents): عندما يساعد الوكيل المتعدد ومتى لا يساعد
- [LangGraph overview](https://docs.langchain.com/oss/python/langgraph/overview): بديل آلة الدولة